<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64" role="img" aria-label="Anjana Yatawara">
  <!-- Deliberately simple: this is read at 16px in a browser tab and in a
       Google result, where the old planet-and-"AY" mark turned to mush.
       One shape, one letter, drawn as a stroked path rather than <text> so it
       never depends on a font being available to the renderer. -->
  <rect width="64" height="64" rx="14" fill="#1a73e8"/>
  <path d="M19 19 L32 35 L45 19 M32 35 L32 47"
        fill="none" stroke="#ffffff" stroke-width="7"
        stroke-linecap="round" stroke-linejoin="round"/>
</svg>
