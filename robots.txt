User-agent: *
Allow: /

# Dev-only files. These are deleted by the deploy workflow before the artifact
# is uploaded, so they should never exist in the web root — this is the second
# lock, not the first. Keep the prefix rules broad so a NEW brief is covered
# the moment it is written.
Disallow: /_

Sitemap: https://www.yatawara.com/sitemap.xml
